{
  "name": "@w6w-apps/surveymonkey",
  "version": "0.1.1",
  "description": "Create and manage SurveyMonkey surveys, responses, collectors and contacts via the SurveyMonkey API.",
  "license": "MIT",
  "author": {
    "name": "w6w"
  },
  "private": true,
  "w6w": {
    "id": "io.w6w.surveymonkey",
    "displayName": "SurveyMonkey",
    "longDescription": "SurveyMonkey puts a survey program's read side into a workflow \u2014 list and read surveys, pull a survey's full page and question detail, create a new survey, read bulk survey responses and drill into one response's detail, list and create collectors, list survey pages, and manage contact lists including adding a new contact.\n\nWriting responses back into SurveyMonkey is deliberately left out: that scope needs the vendor's own approval to use in a public app, and this pack has no case yet that needs it. Webhooks are likewise out of scope for an app that only performs actions, not triggers.\n\nAuthentication is OAuth2, requesting only the scopes the actions above actually use. SurveyMonkey is one of the few vendors in this pack to publish live, real rate-limit headers rather than leaving quota undocumented \u2014 a draft or private app is capped at 120 requests per minute and 500 per day, shared across every user of the OAuth application rather than per individual connection.",
    "categories": [
      "forms",
      "productivity"
    ],
    "appearance": {
      "icon": {
        "svg": "./assets/icon.svg",
        "alt": "SurveyMonkey"
      }
    },
    "network": {
      "allow": [
        "api.surveymonkey.com"
      ]
    },
    "entry": "./index.ts"
  }
}
